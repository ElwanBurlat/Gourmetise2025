version: "3"
services:
  front-app:
    build: .
    container_name: GourmetiseVue
    ports:
      - "3006:3006"
    networks:
      - front-network
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env

networks:
  front-network:
    driver: bridge